# Postgres connection. The default points at the local cluster started by
# `pnpm db:start` (Homebrew) or `docker compose up -d` (Docker variant).
DATABASE_URL=postgres://vulnscope:vulnscope@127.0.0.1:55432/vulnscope
