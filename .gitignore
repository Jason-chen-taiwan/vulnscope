node_modules/
.next/
out/
build/
dist/
.env.local
.env.*.local
.pgdata/
*.log
*.tsbuildinfo
.DS_Store
.claude/

# Private Pro tier code (separate private repo, mounted at runtime)
/pro/
/src/app/[locale]/(pro)/
/src/lib/pro/

# Local-only secrets (deploy keys, etc). Never commit.
/.secrets/

# Oracle VM deploy secrets — filled from env.example on the box, never committed.
/deploy/oracle/.env
/deploy/oracle/cloudflared/

# Output dir of tsconfig.pro.json (in-place compile of /pro)
/.pro-compiled/
.gstack/
/scratch-phase0/

# OpenNext Cloudflare adapter build output + wrangler local state
.open-next/
.wrangler/
